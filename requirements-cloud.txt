# Cloud deployment dependencies for Boomi MCP Server
# Install with: pip install -r requirements-cloud.txt

# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Cloud provider SDKs (install only what you need)

# AWS
boto3>=1.28.0              # AWS SDK (for Secrets Manager, etc.)
botocore>=1.31.0

# Google Cloud
google-cloud-secret-manager>=2.16.0
google-auth>=2.23.0
packaging>=21.0  # Required by google-cloud-secret-manager
proto-plus>=1.22.0  # Required by google-cloud-secret-manager

# Azure
azure-keyvault-secrets>=4.7.0
azure-identity>=1.14.0

# Additional production features
python-multipart>=0.0.6    # For form data parsing
prometheus-client>=0.18.0  # Metrics export
sentry-sdk>=1.34.0         # Error tracking (optional)

# Performance and monitoring
aiofiles>=23.2.1           # Async file operations
orjson>=3.9.10             # Fast JSON serialization
